version: "3.9"
services:
  postgres:
    image: postgres:17.0
    environment:
      POSTGRES_DB: ""
      POSTGRES_USER: ""
      POSTGRES_PASSWORD: ""    
    ports:
      - ":"
    restart: unless-stopped
     volumes: 
      - ../data/postgres:/var/lib/postgresql/data